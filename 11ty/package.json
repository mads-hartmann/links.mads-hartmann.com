{
  "name": "links.mads-hartmann.com",
  "version": "1.0.0",
  "description": "Links to resources I've read or want to read.",
  "main": "index.js",
  "scripts": {
    "dev": "eleventy --config=.eleventy.js --input=. --serve",
    "build": "eleventy build --config=.eleventy.js --input=.",
    "format": "prettier --write src "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.11.1",
    "airtable": "^0.10.1",
    "liquidjs": "^9.20.1",
    "prettier": "^2.2.1"
  },
  "dependencies": {}
}
