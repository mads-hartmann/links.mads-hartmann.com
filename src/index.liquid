---
layout: layout.liquid
title: Links
---

<ul>
    {% for link in airtable %}
        <li>
            <a href="/links/{{ link.title | slug }}">{{link.title}}</a>
        </li>
    {% endfor %}
</ul>
