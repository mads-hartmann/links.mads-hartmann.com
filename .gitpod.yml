image:
  file: .gitpod.Dockerfile

tasks:
  # Tools
  - init: npm install
  # Datasette
  - command: cd datasette && datasette serve -h 0.0.0.0 -p 8001 --metadata metadata.json data/links.db
    openMode: tab-after
  # NextJS
  - init: cd nextjs && npm install
    command: npm run dev
    openMode: tab-after

ports:
  # Datasette
  - port: 8001
    onOpen: notify
    visibility: public
  # NextJS
  - port: 3000
    onOpen: ignore
    visibility: public

github:
  prebuilds:
    master: true
